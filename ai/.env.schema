# AI Environment Variables Schema

This file documents the environment variables used in the AI module. Copy this to `.env.local` and fill in your values.

```bash
# ==========================================
# TensorFlow Configuration
# ==========================================

# GPU Usage (set to 1 to enable, 0 to disable)
TF_GPU_ENABLED=0

# TensorFlow Logging Level
# Options: 0 (all), 1 (info), 2 (warning), 3 (error)
TF_CPP_MIN_LOG_LEVEL=2

# TensorFlow Memory Growth (prevent OOM by allocating dynamically)
TF_FORCE_GPU_ALLOW_GROWTH=true

# ==========================================
# Model Training Configuration
# ==========================================

# Default Model Framework
# Options: tensorflow, sklearn, auto (try TF first, fallback to sklearn)
MODEL_FRAMEWORK=auto

# Random Seed for Reproducibility
RANDOM_SEED=42

# Training Hyperparameters
EPOCHS=15
BATCH_SIZE=32
LEARNING_RATE=0.001
VALIDATION_SPLIT=0.2
TEST_SIZE=0.2

# ==========================================
# Data Processing
# ==========================================

# Default Data Path (relative to project root)
DATA_PATH=datasets

# Default Model Output Path
MODEL_PATH=models

# Default Notebook Path
NOTEBOOK_PATH=notebooks

# ==========================================
# Logging & Monitoring
# ==========================================

# Logging Level for Application
# Options: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# Enable Tensorboard Logging
TENSORBOARD_ENABLED=true

# Tensorboard Log Directory
TENSORBOARD_LOG_DIR=./logs/tensorboard

# ==========================================
# API Integration (Backend)
# ==========================================

# Backend Server URL for Model Serving
API_BASE_URL=http://localhost:3000

# API Key for Model Serving (if needed)
API_KEY=

# Model Registry Endpoint
MODEL_REGISTRY_URL=http://localhost:3000/api/models

# ==========================================
# Database Configuration
# ==========================================

# MongoDB Connection String
MONGO_URI=mongodb://localhost:27017/capstone-ai

# Database Name
DB_NAME=capstone-ai

# ==========================================
# Development Settings
# ==========================================

# Environment Type
NODE_ENV=development

# Jupyter Notebook Port
JUPYTER_PORT=8888

# Jupyter Authentication Token
JUPYTER_TOKEN=

# Enable Debug Mode
DEBUG=false

# ==========================================
# Export Settings
# ==========================================

# Export Format for Models
# Options: h5 (TensorFlow), pkl (scikit-learn), onnx (cross-platform)
EXPORT_FORMAT=h5

# Export Compression Level (for model size optimization)
EXPORT_COMPRESSION=1

# ==========================================
# Optional: Advanced TensorFlow Settings
# ==========================================

# Mixed Precision Training (speeds up training on compatible GPUs)
MIXED_PRECISION=false

# Distributed Training (for multi-GPU setups)
DISTRIBUTED_TRAINING=false

# Number of Parallel Data Loading Threads
NUM_PARALLEL_CALLS=4

# Prefetch Buffer Size for Data Pipeline
PREFETCH_BUFFER_SIZE=32
