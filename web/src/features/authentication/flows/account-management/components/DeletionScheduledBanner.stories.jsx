import DeletionScheduledBanner from './DeletionScheduledBanner.jsx'

export default {
  title: 'Authentication/Account Management/DeletionScheduledBanner',
  component: DeletionScheduledBanner,
  parameters: {
    layout: 'centered',
  },
  tags: ['autodocs'],
}

export const Default = {
  args: {},
}
