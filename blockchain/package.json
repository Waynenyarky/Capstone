{
  "name": "blockchainaudit",
  "version": "1.0.0",
  "description": "Smart contracts for blockchain audit logging - Truffle Suite",
  "main": "main.js",
  "scripts": {
    "compile": "truffle compile",
    "migrate": "truffle migrate",
    "migrate:reset": "truffle migrate --reset",
    "migrate:ganache": "truffle migrate --network ganache && node write-env.js",
    "migrate:development": "truffle migrate --network development && node write-env.js",
    "setup": "truffle migrate --network development --reset && node write-env.js && node GRANT_ROLES.js",
    "test": "truffle test",
    "console": "truffle console",
    "console:ganache": "truffle console --network ganache",
    "develop": "truffle develop",
    "grant-roles": "node GRANT_ROLES.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "chai": "^4.3.10",
    "truffle": "^5.1.67"
  },
  "dependencies": {
    "crypto-js": "^4.2.0",
    "dotenv": "^16.3.1",
    "web3": "^4.0.0"
  }
}
