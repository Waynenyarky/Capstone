import 'package:flutter/material.dart';

class PhotoEvidenceUploadsScreen extends StatelessWidget {
  final String token;

  const PhotoEvidenceUploadsScreen({super.key, required this.token});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Photo & Evidence Uploads'));
  }
}
