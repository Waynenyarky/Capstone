# Mobile App Development Environment
# This file is COMMITTED to git. ./start.sh auto-copies to .env if needed.
# Note: Update BASE_URL to your machine's LAN IP for physical device testing.

BASE_URL=http://localhost:3001
ALT_BASE_URLS=http://localhost:3002
