# Admin Services Components

This folder contains UI components for admin services and categories.

Notes:
- Components were migrated from `organisms` to `components` to align with feature conventions.
- Deep imports like `@/features/admin/services/organisms/*` are no longer supported.
- Prefer importing from the barrel: `@/features/admin/services`.